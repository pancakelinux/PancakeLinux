# PancakeLinux
Debian Testing Based Distro


## Prerequisites

You'll need Debian or Ubuntu (VMs are fine)
```bash
sudo apt update && sudo apt upgrade
sudo apt install -y live-build debootstrap squashfs-tools xorriso isolinux syslinux-utils git
```